# Methods Paper - Chapter 2 Skeleton

This skeleton follows `workflow/capitulo2_pseudobulk_de.R`. The WT/pifq
version should keep the same section order and output structure, changing only
dataset-specific parameters.

The adapted script is:

```text
workflow/capitulo2_wt_pifq.R
```

## Minimal WT/pifq Changes

Use the same Chapter 2 structure with these substitutions:

- `base_dir <- file.path(DATA_DIR, "resultados_wt")`
- input object: `resultados_wt/objects/pbmc_harmony_annotated.rds`
- annotation column: `pseudobulk_annot_col <- "celltype"`
- conditions: `pseudobulk_conditions <- c("WT", "pifq")`
- comparison: `list(conds = c("WT", "pifq"), tag = "WT_vs_pifq")`
- volcano tag: `volcano_tag <- "WT_vs_pifq"`
- QC check example: use `Mesophyll` instead of `Pavement_Cell`
- TF list path: `file.path(DATA_DIR, "SingleCell", "data", "AtTFDB_loci.txt")`

Do not use `celltype_grouped` or `celltype_curated` for this Chapter 2 draft.

## Section Order To Preserve

Keep the same sections as the base pipeline:

- Load annotated object from Part 1
- Section 13 - Cell-type subsets
- Section 14 - Pseudo-replicate assignment
- Section 15 - Pseudobulk tables and DESeq2
- Section 16 - Volcano plots
- Section 17 - Differential gene tables
- Section 18 - GO enrichment
- Section 19 - Log2FC heatmap
- Section 20 - Coexpression network (hdWGCNA) on significant genes
- Section 21 - Network export and visualization
- Section 21b - TF coexpression network

## Current Rendered Draft

The current Chapter 2 methods draft is:

```text
reports/methods_paper_chapter2_wt_pifq.pdf
reports/methods_paper_chapter2_wt_pifq.html
```

The PDF is generated by:

```text
reports/build_methods_chapter2_wt_pifq.py
```

The builder extracts code blocks directly from `workflow/capitulo2_wt_pifq.R`
so the rendered methods stay synchronized with the script structure.

## Figures Included

Only outputs that match the base Chapter 2 workflow are included as small
reference figures:

- representative volcano plot
- representative GO bubble plot
- Section 19 log2FC heatmap

Avoid adding new diagnostic figures unless the base Chapter 2 workflow also
produces them.

## Open Decisions

- Decide whether to run and include the hdWGCNA network figures for WT/pifq.
- Keep the pseudo-replicate limitation visible if this dataset still has one
  library per condition.
- If the user changes the cell type set, update only the minimal parameters
  above, not the section structure.
